ESPmote uses a XIAO ESP32-S3 to behave as a peripheral keyboard and mouse.
The ESP32 plugs into the computer to control.
Then another computer can bluetooth connect to the dongle (via Google Chrome) and behave as keyboard and mouse.

mote.dunc.app
